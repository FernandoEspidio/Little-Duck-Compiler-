program test_vm_division_cero;
var
    resultado : int;
    x : int;
    y : int;

void dividir(a: int, b: int) [
    var temp : int;
    {
        print("Intentando dividir ", a, " entre ", b);
        temp = a / b;
        print("Resultado: ", temp);
        resultado = temp;
    }
];

main {
    x = 100;
    y = 0;
    
    print("=== Test VM: División entre Cero ===");
    dividir(x, y);
    print("Esta línea no debería ejecutarse");
}
end